[user]
	name = Davide Alberani
	email = da@erlug.linux.it
        #signingkey = {GPG key}

[core]
	pager = less -F -X
	editor = vim

[alias]
        st = status
        ss = status --short
        ci = commit
        br = branch
        co = checkout
	df = diff
	ds = diff --cached
        unstage = reset HEAD
        uncommit = reset --soft HEAD^
	lg = log --color --graph --pretty=tformat:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

[diff]
	renames = copies
	mnemonicprefix = true

[push]
	default = simple

[color]
	ui = auto
        diff = auto
        status = auto
        branch = auto
        interactive = auto

[github]
	username = alberanid
	#token = {Github token}

